schema: "../api/src/schemas/types/**/*.graphql"
generates:
  src/graphql/types/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
    config:
      withHooks: true
      skipTypename: true