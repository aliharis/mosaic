schema: "http://localhost:4000/graphql" 
generates:
  src/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
    config:
      withHooks: true
      skipTypename: true