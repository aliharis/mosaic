schema: './src/schemas/types/**/*.graphql'
generates:
  ./src/types/graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      contextType: any
      useIndexSignature: true